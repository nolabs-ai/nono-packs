#!/usr/bin/env bash
# nono-hook.sh - Codex PostToolUse compatibility hook
# Version: 1.7.0
#
# Compatibility no-op. Codex renders hook output in the transcript, so the
# pack now relies on the nono-sandbox skill instead of PostToolUse hooks.
exit 0
